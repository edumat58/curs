---
sidebar_position: 29
slug: /c7/modul-4/03
description: ""
---

# C18.2 - Funcții trigonometrice (proprietăți)

---

## Funcția sinus

:::tip Definiție
$f:\mathbb{R} \to \mathbb{R}$, $f(x) = \sin x$ se numește funcția sinus.
:::

### Proprietățile funcției sinus:

- Perioadă: $2\pi$
> adică $\sin(x + 2\pi) = \sin x$ pentru orice $x \in \mathbb{R}$

- Simetrie: impară
> adică $f(-x) = -f(x)$ pentru orice $x \in \mathbb{R}$

- Mărginită: $f(x) \in [-1, 1]$ pentru orice $x \in \mathbb{R}$

<svg viewBox="0 0 400 200" xmlns="http://www.w3.org/2000/svg" style={{ backgroundColor: 'transparent', width: '100%', height: 'auto' }}>
  {/* Axele de coordonate */}
  <line x1="20" y1="100" x2="380" y2="100" stroke="currentColor" strokeWidth="2" /> {/* Axa X */}
  <line x1="40" y1="10" x2="40" y2="190" stroke="currentColor" strokeWidth="2" />  {/* Axa Y */}
  
  {/* Marcaje Axa Y */}
  <line x1="35" y1="25" x2="45" y2="25" stroke="currentColor" />
  <text x="15" y="30" fontSize="12" fill="currentColor">1</text>
  <line x1="35" y1="170" x2="45" y2="170" stroke="currentColor" />
  <text x="10" y="175" fontSize="12" fill="currentColor">-1</text>

  {/* Marcaje Axa X (Perioada) */}
  <text x="25" y="115" fontSize="12" fill="currentColor">0</text>
  
  <line x1="120" y1="95" x2="120" y2="105" stroke="currentColor" />
  <text x="110" y="120" fontSize="12" fill="currentColor">π/2</text>
  
  <line x1="200" y1="95" x2="200" y2="105" stroke="currentColor" />
  <text x="195" y="120" fontSize="12" fill="currentColor">π</text>
  
  <line x1="280" y1="95" x2="280" y2="105" stroke="currentColor" />
  <text x="270" y="120" fontSize="12" fill="currentColor">3π/2</text>
  
  <line x1="360" y1="95" x2="360" y2="105" stroke="currentColor" />
  <text x="360" y="120" fontSize="12" fill="currentColor">2π</text>

  {/* Graficul Sinus */}
  <path
    d="M 40 100 
       C 80 0, 160 0, 200 100
       C 240 200, 320 200, 360 100"
    fill="none"
    stroke="#098f6d"
    strokeWidth="3"
  />
</svg>

---

## Funcția cosinus
:::tip Definiție
$f:\mathbb{R} \to \mathbb{R}$, $f(x) = \cos x$ se numește funcția cosinus.
:::

### Proprietățile funcției cosinus:
- Perioadă: $2\pi$
> adică $\cos(x + 2\pi) = \cos x$ pentru orice $x \in \mathbb{R}$
- Simetrie: pară
> adică $f(-x) = f(x)$ pentru orice $x \in \mathbb{R}$
- Mărginită: $f(x) \in [-1, 1]$ pentru orice $x \in \mathbb{R}$ 

<svg viewBox="0 0 400 200" xmlns="http://www.w3.org/2000/svg" style={{ backgroundColor: 'transparent', width: '100%', height: 'auto' }}>
  {/* Axele de coordonate */}
  <line x1="20" y1="100" x2="380" y2="100" stroke="currentColor" strokeWidth="1.5" opacity="0.8" />
  <line x1="40" y1="10" x2="40" y2="190" stroke="currentColor" strokeWidth="1.5" opacity="0.8" />
  
  {/* Marcaje Axa Y */}
  <line x1="35" y1="50" x2="45" y2="50" stroke="currentColor" />
  <text x="20" y="55" fontSize="12" fill="currentColor">1</text>
  <line x1="35" y1="150" x2="45" y2="150" stroke="currentColor" />
  <text x="15" y="155" fontSize="12" fill="currentColor">-1</text>

  {/* Marcaje Axa X */}
  <text x="30" y="115" fontSize="12" fill="currentColor">0</text>
  
  <line x1="120" y1="95" x2="120" y2="105" stroke="currentColor" />
  <text x="110" y="120" fontSize="12" fill="currentColor">π/2</text>
  
  <line x1="200" y1="95" x2="200" y2="105" stroke="currentColor" />
  <text x="195" y="120" fontSize="12" fill="currentColor">π</text>
  
  <line x1="280" y1="95" x2="280" y2="105" stroke="currentColor" />
  <text x="270" y="120" fontSize="12" fill="currentColor">3π/2</text>
  
  <line x1="360" y1="95" x2="360" y2="105" stroke="currentColor" />
  <text x="350" y="120" fontSize="12" fill="currentColor">2π</text>

  {/* Graficul Cosinus Corectat */}
  <path
    d="M 40 50 
       C 84 50, 100 80, 120 100
       C 140 120, 156 150, 200 150
       C 244 150, 260 120, 280 100
       C 300 80, 316 50, 360 50"
    fill="none"
    stroke="#e535ab" 
    strokeWidth="2.5"
    strokeLinecap="round"
  />
</svg>

---

## Funcția tangentă
:::tip Definiție
$f:\mathbb{R} \setminus \{\frac{\pi}{2} + k\pi \mid k \in \mathbb{Z}\} \to \mathbb{R}$, $f(x) = \tg x$ se numește funcția tangentă.
:::

### Proprietățile funcției tangentă:
- Perioadă: $\pi$
> adică $\tg(x + \pi) = \tg x$ pentru orice $x \in \mathbb{R} \setminus \{\frac{\pi}{2} + k\pi \mid k \in \mathbb{Z}\}$
- Simetrie: impară
> adică $f(-x) = -f(x)$ pentru orice $x \in \mathbb{R} \setminus \{\frac{\pi}{2} + k\pi \mid k \in \mathbb{Z}\}$
- Nemarginită: funcția tangentă nu este mărginită.

<svg viewBox="0 0 400 300" xmlns="http://www.w3.org/2000/svg" style={{ backgroundColor: 'transparent', width: '100%', height: 'auto' }}>
  {/* Definire zonă de tăiere pentru a preveni ieșirea curbelor din grafic */}
  <defs>
    <clipPath id="graph-area">
      <rect x="0" y="20" width="400" height="230" />
    </clipPath>
  </defs>

  {/* Axele de coordonate */}
  <line x1="20" y1="135" x2="380" y2="135" stroke="currentColor" strokeWidth="1.5" opacity="0.8" /> {/* Axa X */}
  <line x1="40" y1="20" x2="40" y2="250" stroke="currentColor" strokeWidth="1.5" opacity="0.8" />  {/* Axa Y */}
  
  {/* Marcaje Axa Y */}
  <line x1="35" y1="85" x2="45" y2="85" stroke="currentColor" />
  <text x="20" y="90" fontSize="12" fill="currentColor">1</text>
  <line x1="35" y1="185" x2="45" y2="185" stroke="currentColor" />
  <text x="15" y="190" fontSize="12" fill="currentColor">-1</text>

  {/* Marcaje Axa X */}
  <text x="30" y="150" fontSize="12" fill="currentColor">0</text>
  
  {/* Asimptota 1 la pi/2 */}
  <line x1="120" y1="20" x2="120" y2="250" stroke="currentColor" strokeDasharray="5,5" opacity="0.5" />
  <text x="105" y="265" fontSize="12" fill="currentColor">π/2</text>
  
  <line x1="200" y1="130" x2="200" y2="140" stroke="currentColor" />
  <text x="195" y="155" fontSize="12" fill="currentColor">π</text>
  
  {/* Asimptota 2 la 3pi/2 */}
  <line x1="280" y1="20" x2="280" y2="250" stroke="currentColor" strokeDasharray="5,5" opacity="0.5" />
  <text x="265" y="265" fontSize="12" fill="currentColor">3π/2</text>
  
  <line x1="360" y1="130" x2="360" y2="140" stroke="currentColor" />
  <text x="350" y="155" fontSize="12" fill="currentColor">2π</text>

  {/* Graficul Tangentei */}
  <g clipPath="url(#graph-area)">
    {/* Ramura 1: 0 la pi/2 */}
    <path d="M 40 135 Q 100 135 115 20" fill="none" stroke="#25c2a0" strokeWidth="2.5" />
    
    {/* Ramura 2: pi/2 la 3pi/2 */}
    <path d="M 125 250 Q 140 135 200 135 T 275 20" fill="none" stroke="#25c2a0" strokeWidth="2.5" />
    
    {/* Ramura 3: 3pi/2 la 2pi */}
    <path d="M 285 250 Q 300 135 360 135" fill="none" stroke="#25c2a0" strokeWidth="2.5" />
  </g>
</svg>

---

## Funcția cotangentă
:::tip Definiție
$f:\mathbb{R} \setminus \{k\pi \mid k \in \mathbb{Z}\} \to \mathbb{R}$, $f(x) = \ctg x$ se numește funcția cotangentă.
:::

### Proprietățile funcției cotangentă:
- Perioadă: $\pi$
> adică $\ctg(x + \pi) = \ctg x$ pentru orice $x \in \mathbb{R} \setminus \{k\pi \mid k \in \mathbb{Z}\}$
- Simetrie: impară
> adică $f(-x) = -f(x)$ pentru orice $x \in \mathbb{R} \setminus \{k\pi \mid k \in \mathbb{Z}\}$
- Nemarginită: funcția cotangentă nu este mărginită.

<svg viewBox="0 0 400 300" xmlns="http://www.w3.org/2000/svg" style={{ backgroundColor: 'transparent', width: '100%', height: 'auto' }}>
  {/* Definire zonă de tăiere */}
  <defs>
    <clipPath id="cot-clip">
      <rect x="0" y="20" width="400" height="230" />
    </clipPath>
  </defs>

  {/* Axele de coordonate */}
  <line x1="20" y1="135" x2="380" y2="135" stroke="currentColor" strokeWidth="1.5" opacity="0.8" /> {/* Axa X */}
  <line x1="40" y1="20" x2="40" y2="250" stroke="currentColor" strokeWidth="1.5" opacity="0.8" />  {/* Axa Y */}
  
  {/* Marcaje Axa Y */}
  <line x1="35" y1="85" x2="45" y2="85" stroke="currentColor" />
  <text x="20" y="90" fontSize="12" fill="currentColor">1</text>
  <line x1="35" y1="185" x2="45" y2="185" stroke="currentColor" />
  <text x="15" y="190" fontSize="12" fill="currentColor">-1</text>

  {/* Marcaje Axa X (Asimptote și Intersecții) */}
  {/* Asimptota la 0 este chiar axa Y */}
  
  <line x1="120" y1="130" x2="120" y2="140" stroke="currentColor" />
  <text x="110" y="155" fontSize="12" fill="currentColor">π/2</text>
  
  {/* Asimptota la pi */}
  <line x1="200" y1="20" x2="200" y2="250" stroke="currentColor" strokeDasharray="5,5" opacity="0.5" />
  <text x="195" y="265" fontSize="12" fill="currentColor">π</text>
  
  <line x1="280" y1="130" x2="280" y2="140" stroke="currentColor" />
  <text x="265" y="155" fontSize="12" fill="currentColor">3π/2</text>
  
  {/* Asimptota la 2pi */}
  <line x1="360" y1="20" x2="360" y2="250" stroke="currentColor" strokeDasharray="5,5" opacity="0.5" />
  <text x="350" y="265" fontSize="12" fill="currentColor">2π</text>

  {/* Graficul Cotangentei */}
  <g clipPath="url(#cot-clip)">
    {/* Ramura 1: 0 la pi */}
    <path d="M 55 20 Q 70 135 120 135 T 185 250" fill="none" stroke="#ff8c00" strokeWidth="2.5" />
    
    {/* Ramura 2: pi la 2pi */}
    <path d="M 215 20 Q 230 135 280 135 T 345 250" fill="none" stroke="#ff8c00" strokeWidth="2.5" />
  </g>
</svg>

---